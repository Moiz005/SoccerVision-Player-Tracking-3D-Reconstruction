# Colab dependencies — installed via the notebook, not this file directly.
# See notebooks/inference_colab.ipynb for the actual installation steps.
#
# Key versions:
#   torch==2.1.0+cu118 (downgraded from Colab default 2.10.0)
#   tracklab==1.3.24
#   sn-gamestate (editable install, patched pyproject.toml)
#   mmcv==2.1.0 (pre-built wheel)
#   mmdet==3.1.0
#   mmocr==1.0.1
#   easyocr==1.7.1
#
# Installation order:
#   1. pip install torch==2.1.0 torchvision==0.16.0 --index-url .../cu118
#   2. Clone + patch sn-gamestate pyproject.toml
#   3. pip install --no-deps -e sn-gamestate
#   4. pip install tracklab soccernet lightning transformers tokenizers easyocr
#   5. pip install mmcv mmengine mmdet mmocr (from pre-built wheels)
